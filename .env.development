# Local development: call the API through Vite's dev proxy (same-origin),
# so the browser never makes a cross-origin request and CORS never applies.
# The proxy target is configured in vite.config.js.
VITE_API_BASE_URL=/api/v1
VITE_APP_TITLE=Muhammad Umar
VITE_APP_VERSION=1.0.0
